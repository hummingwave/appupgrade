<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="ActivityDialogTheme" parent="Theme.AppCompat.Dialog">
        <!-- Fill the screen -->
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>

    <style name="UpgradeMyDialogTheme" parent="android:Theme.Dialog">
        <!-- Fill the screen -->
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowAnimationStyle">@style/UpgradeAnimation.Window</item>

    </style>
    <style name="UpgradeAnimation.Window" parent="@android:style/Animation.Dialog">
        <item name="android:windowEnterAnimation">@anim/upgrade_pop_up</item>
        <item name="android:windowExitAnimation">@null</item>
    </style>
</resources>